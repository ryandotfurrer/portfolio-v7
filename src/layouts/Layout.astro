---
import Analytics from "@vercel/analytics/astro";
import SpeedInsights from "@vercel/speed-insights/astro";
import Footer from "../components/Footer.astro";
import Navbar from "../components/Navbar.astro";
import "../styles/global.css";
import dirHoverUrl from "../scripts/dir-hover.ts?url";

interface Props {
  title?: string;
  description?: string;
}

const { title = "Ryan Furrer", description } = Astro.props;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="sitemap" href="/sitemap-index.xml" />
    <title>{title}</title>
    {description && <meta name="description" content={description} />}
  </head>
  <body
    class="mx-auto max-w-2xl space-y-16 bg-background px-4 py-8 text-foreground-muted antialiased lg:px-0"
  >
    <Navbar />
    <slot />
    <Analytics />
    <Footer />
    <SpeedInsights />
    <script type="module" src={dirHoverUrl}></script>
  </body>
</html>
