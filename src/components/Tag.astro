---
interface Props {
  label: string;
  href?: string;
}

const { label, href } = Astro.props;
---

{
  href ? (
    <a
      href={href}
      class="font-mono-custom rounded-md bg-card p-1 text-xs lowercase no-underline"
    >
      {label}
    </a>
  ) : (
    <span class="font-mono-custom rounded-md bg-card p-1 text-xs lowercase">
      {label}
    </span>
  )
}
